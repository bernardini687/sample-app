<% provide(:title, 'Fellow humans') %>
<h1>Fellow humans</h1>

<ul class="users">
  <!-- Rails automatically iterates through @users, rendering each user with _user.html.erb -->
  <%= render @users %>
</ul>
<%= will_paginate %>
