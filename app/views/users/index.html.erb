<% provide(:title, 'Fellow humans') %>
<h1>Fellow humans</h1>
<ul class="users">
  <% @users.each do |user| %>
    <li><%= gravatar_for user, size: 50 %></li>
    <li><%= link_to user.name, user %></li>
  <% end %>
</ul>
